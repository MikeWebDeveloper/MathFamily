# ParkMath affiliate-click counter — copy to tools/affiliate/.env (gitignored). READ-ONLY.
# Only needed for `--pull`; --stdin / --file need no credentials.

# Vercel access token (https://vercel.com/account/tokens). Used as a Bearer header only.
VERCEL_TOKEN=""

# math-family-parkmath project + team (defaults are baked in; override if they change).
VERCEL_PROJECT_ID="prj_FOhagNfrcsDa1gm8Uip2A8GbEpUZ"
VERCEL_TEAM_ID="team_19SO92SSblB3MYeywToF6oB0"
